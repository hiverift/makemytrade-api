name: Deploy API

on:
  push:
    branches:
      - main   # jab bhi main branch pe push hoga, ye chalega

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Deploy to Server
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.69.62.78.239 }}
          username: ${{ secrets.root }}
          password: ${{ secrets.AAAAaaaa@@11 }}
          port: 2087
          script: |
            cd /root/makemytrade-api
            git pull origin main
            npm install
            npm run build
            pm2 restart all
